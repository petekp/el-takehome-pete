Step: act-step
Title: Act - implementation relay
Role: implementer
Accepted verdicts: accept

Context (from reads):
--- reports/build/brief.json ---
{
  "objective": "KICKOFF Build Step 4, beat 1 of 7: implement /api/prediction-options (NDJSON envelope, Anthropic tool-use via Sonnet 4.6, exp backoff via withBackoff, returns {framing, options:[{id,label,isCorrect,misconceptionTag}]}) AND upgrade the predict-beat UI to match PRD §3.2 — header \"Your prediction · 1 of 2\" left + End button right, numbered option rows with number badges, free-text textarea sized to one option row with placeholder \"Answer in your own words…\". Wire the endpoint from prototype-store chooseLearn() (replace the registry-fallback static commit). Free-text submission records prediction.freeText (already in the type). End button transitions arc back to idle/chat with no card produced. Reuse misconceptionTag tags allSettled / default-timeout / truth. Keep registry fallback as graceful-degrade path on persistent endpoint failure. Build + lint must stay clean.",
  "scope": "Make the smallest safe change that satisfies the requested goal.",
  "success_criteria": [
    "The requested behavior is implemented",
    "Verification passes",
    "Review completes without a blocking issue"
  ],
  "verification_command_candidates": [
    {
      "id": "npm-check",
      "cwd": ".",
      "argv": [
        "npm",
        "run",
        "check"
      ],
      "timeout_ms": 120000,
      "max_output_bytes": 200000,
      "env": {}
    }
  ],
  "checkpoint": {
    "request_path": "reports/checkpoints/frame-step-request.json",
    "response_path": "reports/checkpoints/frame-step-response.json",
    "allowed_choices": [
      "continue"
    ]
  }
}


--- reports/build/plan.json ---
{
  "objective": "KICKOFF Build Step 4, beat 1 of 7: implement /api/prediction-options (NDJSON envelope, Anthropic tool-use via Sonnet 4.6, exp backoff via withBackoff, returns {framing, options:[{id,label,isCorrect,misconceptionTag}]}) AND upgrade the predict-beat UI to match PRD §3.2 — header \"Your prediction · 1 of 2\" left + End button right, numbered option rows with number badges, free-text textarea sized to one option row with placeholder \"Answer in your own words…\". Wire the endpoint from prototype-store chooseLearn() (replace the registry-fallback static commit). Free-text submission records prediction.freeText (already in the type). End button transitions arc back to idle/chat with no card produced. Reuse misconceptionTag tags allSettled / default-timeout / truth. Keep registry fallback as graceful-degrade path on persistent endpoint failure. Build + lint must stay clean.",
  "approach": "Make the smallest safe change inside scope: Make the smallest safe change that satisfies the requested goal.",
  "slices": [
    "Satisfy: The requested behavior is implemented",
    "Satisfy: Verification passes",
    "Satisfy: Review completes without a blocking issue"
  ],
  "verification": {
    "commands": [
      {
        "id": "npm-check",
        "cwd": ".",
        "argv": [
          "npm",
          "run",
          "check"
        ],
        "timeout_ms": 120000,
        "max_output_bytes": 200000,
        "env": {}
      }
    ]
  }
}


Respond with a single raw JSON object whose top-level shape is exactly: { "verdict": "accept", "summary": "<what changed>", "changed_files": ["<project-relative path>"], "evidence": ["<verification or implementation evidence>"] } Make the smallest behaviorally scoped change that satisfies the requested goal. Do not broaden semantics, normalize data, or add extra behavior just because tests still pass. Use an empty changed_files array only when no file changed. Evidence must contain at least one item. Do not include extra top-level keys. Do not wrap the JSON in Markdown code fences. Do not include any prose before or after the JSON object. The runtime parses your response with JSON.parse, rejects any verdict not drawn from the accepted-verdicts list, and validates the full report body against build.implementation@v1 before writing reports/build/implementation.json.